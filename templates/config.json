{
	{% set comma = joiner(",") %}
	{% for cprovince in registry.keys() %}
	{% for city in registry[cprovince] %}
	{{ comma() }} "{{city[0]}}": "{{names[cprovince]}}/{{city[1]}}.js"
	{% endfor %}
	{% endfor %}
}
